'use client';

/**
 * B-30 — Paramètres — Général org (minimal P1)
 */

export default function ParametresGeneralPage() {
  return (
    <div style={{ padding: 24, maxWidth: 560, margin: '0 auto' }}>
      <h1 style={{ fontSize: 'var(--text-xl)', fontWeight: 600, color: 'var(--text-primary)', margin: 0, marginBottom: 16 }}>
        Paramètres
      </h1>
      <p style={{ fontSize: 'var(--text-sm)', color: 'var(--text-secondary)' }}>
        Configuration de l'organisation. Fonctionnalité disponible dans une prochaine version.
      </p>
    </div>
  );
}
